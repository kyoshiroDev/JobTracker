# schemas-zod

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build schemas-zod` to build the library.

## Running unit tests

Run `nx test schemas-zod` to execute the unit tests via [Vitest](https://vitest.dev/).
